<h1>Administrador de Productos</h1>

<h2>{{#if producto}} Editar producto{{else}} Agregar producto{{/if}}</h2>

<form action="{{#if producto}}/adm-producto/update/{{producto.id}}{{else}}/adm-producto{{/if}}" method="POST">
  <input name="title" placeholder="Nombre" value="{{producto.title}}" required />
  <input name="description" placeholder="Descripción" value="{{producto.description}}" required />
  <input name="code" placeholder="Código" value="{{producto.code}}" required />
  <input name="price" type="number" placeholder="Precio" value="{{producto.price}}" required />
  <input name="stock" type="number" placeholder="Stock" value="{{producto.stock}}" required />
  <input name="category" placeholder="Categoría" value="{{producto.category}}" />
  <input name="images" placeholder="URL de imagen" value="{{producto.images}}" />
  <button type="submit">{{#if producto}} Guardar cambios{{else}}Agregar{{/if}}</button>
</form>
<table border="1" cellpadding="5">
  <thead>
    <tr>
      <th>Título</th>
      <th>Precio</th>
      <th>Stock</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    {{#each productos}}
      <tr>
        <td>{{this.title}}</td>
        <td>${{this.price}}</td>
        <td>{{this.stock}}</td>
        <td>
          <form action="/adm-producto/delete/{{this.id}}" method="POST" style="display:inline;">
            <button type="submit">🗑 Eliminar</button>
          </form>
         <form action="/adm-producto" method="GET" style="display:inline;">
  <input type="hidden" name="edit" value="{{this.id}}" />
  <button type="submit">Editar</button>
</form>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
